version: "3.1"

services:
  valheim_server:
    build: .
    image: valheim
    environment:
      - WORLD_NAME=${WORLD_NAME}
      - WORLD_PASSWORD=${WORLD_PASSWORD}
    ports:
      - ${PORT_RANGE}:2456-2458/udp
    volumes:
      - ./worlds/${WORLD_NAME}:/valheim/worlds
